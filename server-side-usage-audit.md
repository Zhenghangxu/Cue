  按优先级，有以下问题：

  1. 高风险：开放访问后，任何能访问 API 的人都能操作媒体库。
     没有登录或权限校验。访问者可以改设置、提交付费翻译任务、重命名视频。现有 Host／Origin 检查用于
     限制浏览器请求，并不验证用户身份；测试中，无身份凭据的请求仍能创建任务，返回 202。
     另外，目录选择接口允许浏览进程有权限访问的服务器目录，设置接口还能更换媒体根目录。不能把当前配
     置的媒体目录当成安全边界。见 API 防护 (backend/app.py:1980)、目录浏览 (backend/app.py:2060)。

  2. 高风险：修改服务地址可以间接泄露已保存的密钥。
     设置更新会保留旧密码和 API key，但允许修改 WebDAV／AI 服务地址。攻击者不需要读出密钥，只要把地
     址换成自己控制的服务，再触发对应请求，就可能收到原有认证信息。地址校验也允许内网地址，具有服务
     器代发请求的风险。
     这是代码层面的确认，未进行真实凭据外传测试。见 设置更新 (backend/app.py:2140)、AI 客户端初始化
     (backend/app.py:1856)。

  3. 高风险：排队期间切换媒体库，可能处理或重命名错文件。
     任务保存的是相对路径，实际开始执行时才读取全局媒体源。例如在库 A 提交 Movie.mkv，等待期间切换到
     库 B，任务会处理 B 中的同名文件。已用 mock 隔离测试复现。
     应在入队时绑定媒体源和配置，或禁止有未完成任务时切换相关设置。见 任务执行 (backend/
     app.py:1856)。

  4. 部署阻断：默认无法通过服务器 IP／域名正常访问。
     启动命令只监听 127.0.0.1，后端也只接受 localhost Host。测试中，服务器域名返回 400；反向代理如果
     把 Host 改成 localhost，却保留外部 Origin，写请求返回 403。
     需要一起设计监听地址、允许的域名、代理转发和身份验证，单改成 0.0.0.0 不够。见 启动命令
     (package.json:8)、Host 设置 (backend/app.py:1965)。

  5. 可靠性：任务不持久化，也不支持多个 worker／实例。
     队列和任务记录都是进程内存：重启会丢失；多个 worker 各自持有任务，查询可能落到另一进程而返回
     404，重复任务检查也无法跨进程生效。任务数量、批次大小和历史记录没有上限，长期运行会积累内存占
     用。见 队列定义 (backend/app.py:1830)、任务提交 (backend/app.py:2292)。

  6. media server 特有影响：Smart Rename 只改视频名。
     没有同步重命名同名字幕、NFO 或封面。例如 Old.mkv 改成 New.mkv，Old.zh-cn.srt 会留下，可能失去自
     动匹配。正在使用旧路径的播放器或媒体索引也可能受影响。另外，本地重命名依赖硬链接，不支持硬链接
     的挂载会失败。见 重命名实现 (backend/app.py:1473)、本地移动实现 (backend/app.py:1040)。

  还有几个使用上的落差：

  - “打开文件夹／凭据文件”是在服务器调用文件管理器；无桌面的 NAS／容器通常会失败。
  - 任务 ID 保存在浏览器 sessionStorage，另一设备无法自动看到已有任务；后端没有任务列表接口。
  - 配置保存在运行用户的 ~/.config/subtitle-maker/，容器部署需要持久化这个目录并正确设置媒体挂载权
    限。

  - 前端多处使用根路径 /api、/sw.js，部署在 /cue/ 这样的子路径需要额外适配。

  如果只供你自己在家里使用，可以先采用受认证保护的独立域名入口、单进程运行、低权限服务账号和有限的媒
  体目录挂载；优先修复队列绑定与重命名配套文件问题。若要给多人使用，还需要完整的权限和持久化任务设
  计。

  验证结果：后端测试 87 项，86 项通过、1 项跳过。以上是源码与隔离测试审计，尚未验证真实 NAS、反向代
  理或媒体服务器联动。
